pick db2f733 COM-1944 Update saving of units to transition quote if the status is FAILED

# Rebase 2e97355..db2f733 onto 2e97355 (1 command(s))
#
# Commands:
# C-c C-c  tell Git to make it happen
# C-c C-k  tell Git that you changed your mind, i.e. abort
# M-x git-rebase-backward-line move point to previous line
# M-x forward-line move point to next line
# M-p      move the commit at point up
# M-n      move the commit at point down
# M-x git-rebase-show-or-scroll-up show the commit at point in another buffer
# M-x git-rebase-show-commit show the commit at point in another buffer and select its window
# u        undo last change
# M-x git-rebase-kill-line drop the commit at point
# M-x git-rebase-insert insert a line for an arbitrary commit
# M-x git-rebase-noop add noop action at point
#          pick = use commit
#          reword = use commit, but edit the commit message
#          edit = use commit, but stop for amending
#          squash = use commit, but meld into previous commit
#          fixup = like "squash", but discard this commit's log message
#          exec = run command (the rest of the line) using shell
#
# These lines can be re-ordered; they are executed from top to bottom.
#
# If you remove a line here THAT COMMIT WILL BE LOST.
#
# However, if you remove everything, the rebase will be aborted.
#
# Note that empty commits are commented out
